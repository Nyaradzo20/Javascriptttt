for(let square = 1; square <=5; square++){
    //outer loop for rows
    line ='';
    for(let t = 1; t <=4; t++){
     line +='*';
     //inner loop for columns
     //console.log adds a new line, therefore we use line +=
    }
    console.log(line);

     
}