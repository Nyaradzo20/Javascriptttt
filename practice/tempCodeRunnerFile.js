for (let row = 1; row <=5; row++){
        //outer loop for rows
        line ='';
        for (let rightangle = 1; rightangle <= row; rightangle++)
            {
         line +='*';
         
        }
        console.log(line);
}